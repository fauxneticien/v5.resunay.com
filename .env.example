# Cloudflare credentials for `task push` (wrangler pages deploy).
# Copy to .env and fill in. .env is gitignored. Only needed for push, not build/preview.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
